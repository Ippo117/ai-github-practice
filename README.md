# ai-github-practice

A small React arithmetic trainer built with Vite.

Features:
- Starts with easy arithmetic problems
- Increases difficulty after each correct answer
- Introduces larger numbers and harder operators over time
- Lets the player reset back to the easiest difficulty

Commands:
- `npm install`
- `npm run dev`
- `npm test`
- `npm run build`
